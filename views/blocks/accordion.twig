<div
    class="accordion"
    data-colour-scheme="{{ block.colour }}"
    data-block-spacing-top="lg"
    data-block-spacing-bottom="lg"
>
    <InnerBlocks
        template="{{ block.template }}"
        allowedBlocks="{{ block.allowed_blocks }}"
    />
</div>
