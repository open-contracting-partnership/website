{% extends "base.twig" %}

{% block content %}
    <main class="page-container / block-spacing block-spacing--with-start-margins block-spacing--with-end-margins">
        {% if not hide_title %}
            <h1>{{ title }}</h1>
        {% endif %}

        {{ content | raw }}
    </main>
{% endblock %}
