{% extends "base.twig" %}

{% block content %}

	<main class="page-container / event-single">

		<div class="event-single__header">

			{% if back_link %}

				<a class="arrow-link" href="{{ back_link.url }}" data-direction="west" data-text-color="black">
					<svg class="arrow-link__icon"><use xlink:href="#icon-arrow-circle"></use></svg>
					<span class="arrow-link__label / uppercase">{{ back_link.label }}</span>
				</a>

			{% endif %}

		</div>
		
		<div class="event-single__event">
			{% include 'partials/event.twig' with event %}
		</div>

	</main>

{% endblock %}
